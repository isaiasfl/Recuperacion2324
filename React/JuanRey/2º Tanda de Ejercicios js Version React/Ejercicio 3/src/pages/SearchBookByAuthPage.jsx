import SearchBookByAuthForm from '../components/SearchBookByAuthForm';

const SearchBookByAuthPage = () => {
    return (
        <div>
        <h2>Buscar Libros por Autor</h2>
        <SearchBookByAuthForm />
        </div>
    );
};

export default SearchBookByAuthPage;