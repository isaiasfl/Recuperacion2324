

const reservaHabitaciones = () => {
  let habitaciones = new Map();

  const addHabitacion = () => {
    let id = habitaciones.size + 1;
    const habitacion = ({
      nombreUsuario: nombreUsuario,
      fechaEntrada: fechaEntrada,
      fechaSalida: fechaSalida,
      estadoreserva: false,
      precio: precio
  });
  }
}